@* <div class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Modal title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <EditForm Model="Student" OnValidSubmit="HandleCreate">
                <DataAnnotationsValidator />
                <div class="form-group mt-2">
                    <label for="firstName">First Name</label>
                    <InputText class="form-control" @bind-Value="Student!.FirstName" placeholder="Student's first name" />
                    <ValidationMessage For="@(() => Student.FirstName)" />
                </div>
                <div class="form-group mt-2">
                    <label for="lastName">Last Name</label>
                    <InputText class="form-control" @bind-Value="Student.LastName" placeholder="Student's last name" />
                </div>
                <div class="form-group mt-2">
                    <label for="dateOfBirth">Date of Birth</label>
                    <InputDate class="form-control" @bind-Value="Student!.DateOfBirth" />
                </div>
                <div class="form-group mt-2">
                    <label for="typeOfStudy">Type of study</label>
                    <InputSelect class="form-control" @bind-Value="Student.TypeOfStudy">
                        <option value="typeOfStudy">-- Select Type of study --</option>
                        @foreach (var type in Enum.GetValues(typeof(StudyTypeEnum)).Cast<StudyTypeEnum>().ToList())
                        {
                            <option value=@type.ToString() >@type</option>
                        }
                    </InputSelect>
                    <ValidationMessage For="@(() => Student.TypeOfStudy)" />
                </div>
            <div class="modal-body">
                <p>Modal body text goes here.</p>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-primary mt-2"><span class="oi oi-pencil" aria-hidden="true"></span> Create book</button>
                <button type="button" data-dismiss="modal" class="btn btn-secondary mt-2"><span class="oi oi-media-skip-backward" aria-hidden="true"></span> Back To List</button>
            </div>
            </EditForm>
        </div>
    </div>
</div> *@